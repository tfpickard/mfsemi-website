export async function fetchLiveStats() {
  // Simulate fetching live stats with random numbers.
  return {
    quantum: Math.floor(Math.random() * 1000),
    flux: Math.floor(Math.random() * 500),
  };
}
